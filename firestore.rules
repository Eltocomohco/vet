rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas de DESARROLLO - Permitir todo a usuarios autenticados
    // TODO: Reemplazar por reglas de produccion antes de lanzar
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
